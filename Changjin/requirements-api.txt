# Runtime dependencies for the FastAPI service (see Dockerfile).
#
# Deliberately not requirements.txt: that file carries torch (~315 MB) for the
# ChemBERTa preprocessing scripts, plus pandas, openpyxl and beautifulsoup4 for
# the data-preparation tooling. None of it is reachable from the endpoints the
# container serves, and torch alone would dominate the image.
#
# rdkit ships manylinux x86_64 wheels, so no Conda environment is needed here.
fastapi>=0.110
uvicorn[standard]>=0.27
duckdb>=0.10
pydantic>=2.0
rdkit>=2023.9
numpy>=1.24
