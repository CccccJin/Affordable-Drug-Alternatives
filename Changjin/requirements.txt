# NOTE: RDKit is NOT listed here because reliable wheels are not available on PyPI (especially on macOS arm64).
# Please create the Conda env using `environment.yml` to install RDKit and other deps:
#   conda env create -f environment.yml && conda activate chemapi
# This requirements.txt is a fallback for pip-only workflows that do not need RDKit.
fastapi
uvicorn[standard]
duckdb>=0.6.0
pandas>=1.3.0
chembl_webresource_client
beautifulsoup4>=4.10.0
requests>=2.26.0
openpyxl>=3.0.9

# Dependencies for ChemBERTa-based service
torch>=1.9.0
numpy>=1.21.0
transformers>=4.30.0

# Dependencies for drug price aggregation
scikit-learn>=1.0.0